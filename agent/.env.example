AGENT_PORT=7590
RUST_API_URL=http://127.0.0.1:7587
# The browser forwards its existing Mengnex HttpOnly session cookie to the local gateway.
AGENT_EXECUTION_MODE=approve_high_risk
AGENT_ALLOWED_CAPABILITIES=media.search,tasks.read,tasks.create,media.import,system.health
