# Mengnex Knowledge Base

Place trusted Markdown or plain-text documents in this directory. The Knowledge Base plugin only indexes files beneath this directory, and exposes them through the `knowledge.search` tool after the `knowledge.read` capability is allowed.
