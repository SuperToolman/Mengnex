# 更新日志

本文档记录 Mengnex 的重要变更。

## 0.46 - 2026-08-20

#### 新增

- 新增 Agent 模型供应商独立设置页 `/settings/agent/models`，为本地媒体库用户提供面向业务的模型连接管理入口。
- 新增 DeepSeek、OpenAI 和 Ollama 的 OpenAI-compatible 快速配置预设，并支持自定义兼容服务地址。
- 新增模型供应商实例的卡片列表，展示当前模型、接口地址、启用状态、默认状态和密钥配置状态。
- 新增供应商实例的添加、编辑、启用/停用、设置默认和删除操作，继续通过 `openai-compatible-provider` 插件动作 API 执行。

#### 变更

- Agent 设置导航增加“模型供应商”入口，模型连接配置从通用插件扩展页中独立出来。
- 明确区分模型协议插件与模型供应商实例：插件页负责 Cordis 插件生命周期，模型页负责连接地址、模型、凭据和默认选择。
- 前端新增类型化模型供应商 API 封装，统一复用 Agent Gateway 的认证、错误处理和插件动作通道。
- 模型编辑时 API Key 默认保持已保存值，不回显到浏览器；只有输入新值时才更新密钥。

#### 优化

- 模型配置表单使用 HeroUI 控件和 Card 布局，与 Mengnex 设置页的交互和视觉体系保持一致。
- 常用供应商通过预设自动填充名称、Base URL 和模型名，减少首次配置步骤，同时保留完全自定义能力。
- 页面说明明确 Ollama、本地模型、远程 OpenAI-compatible 服务和协议插件之间的职责边界。

#### 修复

- 修复模型供应商配置隐藏在插件扩展页、客户难以发现和管理的问题。
- 修复前端模型供应商操作可能绕过 Agent 插件动作层的问题，所有管理操作统一走 `openai-compatible-provider` 的受控接口。

## 0.45 - 2026-08-20

#### 新增

- 新增媒体领域 capability seam：媒体目录、媒体库访问、扫描、任务、元数据和外部导入均有独立 Cordis 服务契约。
- 新增 `rust-media-capabilities` 插件，将 Rust API 封装为可替换的媒体领域 adapter。
- 新增 seam 替换和媒体库权限拒绝测试。
- 新增 Agent 模型供应商管理页面 `/settings/agent/models`，支持 DeepSeek、OpenAI、Ollama 快速填充、自定义 OpenAI-compatible 连接、编辑、启停、设为默认和删除。

#### 变更

- `core-tools` 不再直接依赖 `RustApiClient`，所有媒体工具改由领域 capability 调用。
- 扫描和外部导入的库权限检查集中到 `libraryAccess`，能力白名单改为领域命名。
- 模型供应商页面与插件生命周期页面分离：前者管理供应商实例，后者管理协议插件的挂载和配置。

#### 修复

- 修复媒体 capability 服务直接 `new` 导致 Cordis 服务生命周期和依赖注入不完整的问题，统一通过 `ctx.plugin()` 安装和反向卸载。

## 0.44 - 2026-08-19

#### 新增

- 新增可替换的 `gateway` capability。HTTP Gateway 通过统一门面访问会话、聊天、工具、审批、插件与观测服务，不再直接依赖多个具体 Cordis 服务名。
- 新增持久化单机 scheduler：作业状态、执行次数、错误、指数退避重试与运行历史保存到 `agent/data/jobs.json`；重启时中断的 running 作业会重新排队，等待所属插件注册 handler 后继续。
- 新增 Scheduler HTTP 观测接口，支持当前用户查看、创建和取消作业；调度器通过 typed events 广播 queued、started、retrying、completed、failed 与 cancelled 状态。
- 新增可替换的本地进程 sandbox：禁用 shell、使用每次独立工作目录、命令白名单、环境收敛、运行超时和输出上限，并记录执行结果事件。
- 新增版本化事件信封和 JSONL 审计记录，事件包含 UUID、版本、来源、时间戳和 payload；支持按类型/来源回放，监听器失败不会中断其他监听器。
- 新增插件包版本发现，支持扁平目录及 `plugins/<plugin-id>/<version>/` 仓库目录；同一插件会选择最高已发现版本。
- 新增插件安装版本、来源、更新可用状态及最近 10 个配置/启停快照；设置页支持更新到已发现版本和回滚历史快照。
- 新增 scheduler、sandbox 与事件总线测试，验证调度重试、持久化历史、工作目录隔离、命令拒绝、事件版本化、持久化及监听器失败隔离。

#### 变更

- 插件浏览器 UI 从原生 DOM 脚本迁移为宿主托管的 React/HeroUI runtime；插件通过 `ctx.ui.React` 和 `ctx.ui.components` 注册视图，宿主统一负责挂载、主题上下文与卸载。
- 模型供应商和执行策略的复杂设置页改为 HeroUI Card、TextField、Switch、Select、Button 组件；声明式插件 schema 也统一由 HeroUI 控件渲染。

- 移除 `builtins.ts` 兼容入口；入口直接注册 `core-plugins.ts` 中的核心 Cordis 插件定义。
- 本地 sandbox 和 scheduler 从占位 capability 调整为可运行实现，同时继续通过 `sandbox`、`jobs` 独占 slot 支持容器、远程执行器或分布式调度器替换。
- 插件依赖支持 `plugin@version-range` 形式的基础版本约束，安装时校验已发现依赖版本。
- 插件状态统一为 `enabled`、核心插件默认字段统一为 `defaultEnabled`，本地包 manifest 统一使用 `default_enabled`；旧 `installed` 状态格式不再读取或迁移。
- 模型供应商与执行策略只读取独立的 `providers.json` 和 `policy.json`，不再回退读取旧 `settings.json` 或环境变量策略。

#### 优化

- 插件、会话、模型、策略、循环与工具实现现在均处于 Gateway Facade 之后；替换实现只需满足 capability 契约，不再要求修改 HTTP 路由。
- 插件管理页面展示已安装版本，并在检测到本地已发现新版本时提供更新操作；扩展设置页展示可回滚历史。

#### 修复

- 移除 Agent 插件设置中的 `innerHTML`、`querySelector`、`prompt` 及原生表单控件路径，避免插件 UI 与主 Web 主题、交互规范不一致。
- 修复 Agent typed event 服务错误占用 Cordis 内部 `events` 名称、导致 Gateway 启动时 `root.events.load is not a function` 的问题；服务名调整为独立的 `agentEvents` capability。
- 移除一键启动脚本对旧版 `3000`、`3001`、`3010` 端口的兼容清理提示，避免正常启动时产生误导性的 Legacy 端口输出。
- 修复事件审计文件使用字面量 `\\n` 而无法作为 JSONL 正确回放的问题。
- 修复事件监听器抛出异常会影响后续监听器执行的问题；失败会被独立记录为 `event:listener_failed`。

## 0.43 - 2026-08-18

### 新增

- 新增独立的 Cordis Agent Gateway（默认 `http://localhost:7590`），通过现有 Rust API 和登录会话受控访问 Mengnex，不直接读写 SQLite、媒体文件或 WebDAV 凭据。
- 新增 `/agent` 对话页，支持创建会话、发送消息、模型工具调用、执行结果展示与需要审批时的继续执行。
- 新增 Agent 插件管理与扩展设置页面，支持查看本地已发现插件的状态、启用/停用、依赖关系、权限声明和插件配置；复杂插件可提供受信任的浏览器端设置模块。
- 新增模型供应商配置与管理能力，支持 OpenAI 兼容接口、多供应商启停、设为默认、更新与移除，并将供应商配置持久化在 Agent 本地数据目录。
- 新增 Agent 执行策略：`request_approval`、`approve_high_risk` 与 `full_access`，并保留高危/关键能力的审批控制和能力白名单。
- 新增 Agent 会话、工具调用记录与审批状态的本地持久化；会话按 Mengnex 登录用户隔离，Gateway 重启后仍可处理待决审批。
- 新增 `POST /api/media/import` 外部导入接口，以 `library_id + source + external_id` 幂等创建或更新外部媒体占位记录，并沿用现有媒体库写入权限检查。
- 新增 Cordis 插件包发现与加载机制，支持本地受信任的 Hello World、技能、知识库和 MCP 客户端插件示例；知识库可检索 `agent/knowledge` 下的资料，MCP 可发现已配置 stdio 服务的工具。
- 新增插件 capability seam：工具、模型、存储、会话、Agent Loop、技能、知识库、MCP、沙盒、调度和 UI 均以插件声明的服务、依赖与能力进行组合。
- 新增 typed Agent events、插件依赖级联启停和可替换的独占 capability slot；模型与执行策略内建实现已作为可组合插件接入。
- 新增 profile/overlay 组合配置，可通过运行时配置叠加插件启用状态和插件配置，无需改动 Gateway 源代码。
- 新增 API、文档、前端和 Agent 的统一 `start.ps1` 启动流程，以及 Agent 的构建和测试 CI 任务。
- 新增 Agent Gateway 的会话与运行 HTTP 接口：会话列表、创建、详情、消息提交、直接运行工具、审批决策、工具列表、插件列表、插件配置和插件客户端模块加载。
- 新增受认证保护的 Agent 管理权限边界：会话和工具执行要求登录用户，插件与策略配置要求 Owner 或 Admin。
- 新增 `agent/data/providers.json`、`sessions.json`、`approvals.json`、`plugins.json` 和 `policy.json` 等独立运行状态文件，避免与 Rust 应用数据耦合。
- 新增 OpenAI Compatible LLM Adapter，能够将注册工具转换为模型函数定义，并执行有上限的“模型响应 -> 工具调用 -> 模型响应”循环。
- 新增核心 Mengnex 工具插件，覆盖 `media.search`、`tasks.list`、`tasks.create_scan` 和 `media.import`，所有操作继续经过 Rust API 的现有账户和媒体库权限检查。
- 新增 Agent 上下文服务，允许 Skill 插件以可释放的注册项向对话注入系统指令，避免将技能逻辑硬编码到 Gateway。
- 新增 Plugin API 服务，供插件向 Web 设置页暴露受控动作；模型供应商和执行策略均通过该服务提供读取、创建、更新、删除或设为默认等管理动作。
- 新增本地插件 manifest，声明插件 id、版本、类型、入口、依赖、提供能力、权限、配置 schema、可选 UI 贡献和独占 slot。
- 新增插件配置 schema 校验，支持字符串、数值、布尔值、枚举、数组与对象字段，并阻止不允许的未知配置项。
- 新增 Hello World 插件示例及其健康检查工具，作为本地插件发现、挂载、停用和卸载生命周期的最小可运行样例。
- 新增受信任的 MCP stdio 客户端插件，可完成 MCP 初始化、发现 `tools/list` 工具并以 `mcp.<server>.<tool>` 名称注册到 Agent 工具表。
- 新增知识库插件的受限路径解析和文本检索能力，拒绝跳出 `agent/knowledge` 根目录的配置路径。
- 新增 `cordis.json` 组合配置及 `AGENT_PROFILE`、`AGENT_OVERLAYS` 运行时选择变量，支持多个 overlay 按顺序覆盖同一插件配置。
- 新增 Agent 单元测试，覆盖执行策略、工具注册/调用、会话持久化、审批恢复、插件状态恢复、slot 替换、依赖级联、插件加载、浏览器 UI、typed events 和组合配置。

### 变更

- 后端、API 文档、前端与 Agent Gateway 默认端口分别调整为 `7587`、`7588`、`7589`、`7590`；Swagger/OpenAPI 文档从业务 API 监听器拆分到独立端口。
- 后台扫描与媒体信息生成从请求内 `tokio::spawn` 改为先持久化为 queued 任务，再由进程内数据库轮询执行器领取和运行；任务状态由 queued 转为 running，重启后的未完成任务仍会被标记为失败。
- 音乐专属表不再在每次连接数据库时执行重建；仅在首次创建、结构不完整或显式设置 `MENGNEX_RESET_MUSIC_SCHEMA=1` 时重建。
- Web 端音乐 API 调用改为使用 OpenAPI 生成的类型安全 SDK，抽离统一传输层处理 URL 规范化、凭据和错误处理，移除通用 JSON 请求绕过路径。
- 设置页增加 Agent 分组，侧边栏增加 Agent 入口；音乐请求逻辑按 feature 收敛，降低通用 API 门面规模。
- Agent 的启动入口由直接组装业务服务调整为“创建 Cordis Context -> 注册内建与本地插件 -> 应用 profile/overlay -> 启动已启用插件 -> 暴露 HTTP 网关”的顺序，Gateway 自身不再拥有模型、工具或策略的业务实现。
- 模型供应商和执行策略从散落的 Gateway 内建逻辑调整为可替换的 capability 插件；模型占用 `model` 独占 slot，策略通过 Plugin API 向设置页提供交互。
- 插件状态从“是否已安装”的内部兼容字段收敛为“本地包是否被发现 + 是否启用”的实际语义；浏览器不再被设计为上传、安装或执行任意插件代码。
- Agent 设置导航由旧的模型供应商/策略专用页面收敛到插件与扩展设置入口，模型和策略作为插件自身的配置界面呈现。
- 插件设置从空白 JSON 文本框调整为读取持久化配置后的 schema 表单；仅在数组或对象等复杂字段中保留 JSON 编辑交互。
- 前端 Agent 请求由 Rust API 客户端分离到独立 feature API，改为直连本地 Agent Gateway 并携带 Cookie，避免将 Agent 管理路由误代理到 Rust API。
- API 主监听器只承载业务接口，Swagger UI 与 OpenAPI JSON 独立由文档监听器提供；CORS、Next.js 代理和图片远程地址同步迁移至新端口。
- 后台任务启动语义由“创建即运行”调整为“创建即入队”，扫描任务与 `app_tasks` 均先写入 `queued` 状态，再由 worker 统一执行。
- 缓存任务的创建与重试不再各自派生执行协程，统一交由后台 worker 根据任务类型分派照片、视频、音乐或小说处理逻辑。

### 优化

- 插件生命周期由 Cordis 内核统一管理，插件以声明式依赖、提供服务、权限和配置契约协同；替换占用同一 capability slot 的插件时会暂停依赖项、完成替换后恢复依赖链。
- Agent 工具名称按模型函数调用规范标准化，保证名称仅使用字母、数字、下划线或连字符。
- 插件配置页会加载并展示当前持久化配置；具有声明式配置契约的插件使用表单渲染，复杂插件可挂载自定义交互 UI。
- 后台任务执行并发继续受全局上限控制，任务创建后可由统一 worker 调度，减少扫描和缓存路径各自维护执行逻辑造成的状态分叉。
- CI 在 Web 构建前同步 OpenAPI 并校验导出的规范和生成 SDK 无差异，避免接口契约与前端调用层漂移。
- Agent Gateway 对来自 `7589` 的浏览器请求提供凭据 CORS 支持，并为其他来源采用固定本地默认源，保证本地三服务联调时会话能够正确转发。
- 模型供应商状态维护默认项唯一性：设置新的默认供应商会自动撤销旧默认项；停用或删除默认项前必须先选择其他可用供应商。
- Provider 配置文件写入采用串行队列与仅所有者可读写权限，降低并发设置请求覆盖状态的概率。

- 插件状态写入采用串行队列；插件启动、停止与工具执行通过串行 typed events 广播，便于后续审计、指标和扩展监听器接入。
- 插件启动会先递归启动依赖并检测循环依赖；停用或替换基础插件时按依赖逆序卸载，避免残留已失效服务引用。
- 浏览器端复杂插件以受认证的 JavaScript 模块按需加载，并由宿主提供有限的 `register()` 接口，而不是将动态 UI 逻辑混入通用设置页。
- MCP 工具调用统一标记为高风险 capability，仍会经过执行策略和审批流，而不会绕过 Agent Runtime。
- 外部媒体导入将媒体项与媒体文件写入放入同一数据库事务；重复导入更新现有记录并清除失效标记，避免出现重复占位项。
- 音乐候选元数据排序改为稳定的逆序 key 排序，减少复杂比较函数带来的可读性和 lint 风险。

### 修复

- 修复音乐索引、收藏、歌单及播放状态会在每次数据库连接时被清空的问题。
- 修复模型供应商更新时传入空 API Key 被误作为有效密钥的问题；现在必须显式清除已有 Key。
- 修复 `/agent` 工具调用向模型传递不符合函数名称约束的名称而导致请求失败的问题。
- 修复前端 `/settings/agent/provider` 等 Agent 请求未正确转发现有登录 Cookie、与 Rust API 混用而出现 401 的问题，统一改由 Agent Gateway 代理管理接口处理。
- 修复扫描和缓存任务仅依赖短生命周期 spawn、创建后可能没有统一执行器领取的问题。
- 修复 API 文档仍与业务 API 共用旧端口、启动脚本无法独立探测文档监听器的问题。
- 修复端口调整后 Next.js 代理、媒体远程图片地址、Rust CORS 白名单和 Agent CORS 白名单仍指向旧 `3000/3001` 默认地址的问题。
- 修复插件停用或替换时仍存在活动依赖插件、可能继续调用已卸载服务的问题；现在会执行依赖级联暂停与恢复。
- 修复插件配置页不显示已保存配置，导致用户无法判断当前 Skill、MCP 或知识库配置的问题。
- 修复模型供应商没有可见的历史列表、启停和默认项管理能力的问题。
- 修复 Agent 模型工具调用使用显示名称或点号名称时不符合部分 OpenAI Compatible Provider 的函数命名正则的问题。
- 修复任务创建时立即标记为 running、应用在任务尚未被实际执行时异常退出会留下误导性运行状态的问题。
- 修复音乐表仅部分存在时初始化逻辑不完整的问题；现在会在检测到结构缺失时一次性重建关联表。
- 修复 Web 端音乐 folders、stats、lyrics 与浏览筛选请求绕过 OpenAPI 类型定义和统一错误处理的问题。

## 0.42 - 2026-08-17

### 新增

- 新增小说媒体库首版，支持扫描本地目录和 WebDAV 数据源中的 EPUB、TXT 文件。
- 新增小说领域数据模型：小说元数据、章节索引和用户级阅读进度。
- 新增 EPUB 元数据、作者、语言、简介、封面与书脊章节解析；TXT 文件以全文章节形式提供阅读。
- 新增小说后台解析任务，复用统一任务中心的进度、暂停、继续、取消和失败状态能力。
- 新增小说 API、OpenAPI 定义与生成的 Web 客户端 SDK，提供书架、详情、章节、封面和阅读进度接口。
- 新增 `/novel` 书架、`/novel/{id}` 详情页和 `/novel/read/{id}` 沉浸式阅读器，支持章节切换、字号调整和自动保存阅读进度。
- 新增独立 `NovelCard` 组件，使用 HeroUI Card、Chip、Tooltip 及项目主题 token 展示小说封面、标题、作者、格式和章节数。
- 新增音乐媒体库领域模型，覆盖专辑、曲目、艺人及多艺人关联、播放进度、收藏和用户歌单。
- 新增音乐扫描后的标签解析，支持标题、专辑、Album Artist、流派、年份、作曲/作词/制作人、内嵌封面、内嵌歌词及本地同名 `.lrc` 歌词。
- 新增音乐 API、OpenAPI 定义与 Web 客户端门面，提供专辑、艺人、歌曲、收藏、最近播放、歌单、统计、文件夹、歌词和音频质量数据。
- 新增全局 `MusicPlayerProvider`，支持跨页面播放队列、顺序/随机/单曲循环、上一首/下一首、音量、进度拖动和播放进度保存。
- 新增 `/music` 音乐首页、专辑、歌曲、艺人、文件夹、收藏、最近播放和歌单视图，以及艺人详情页。
- 新增歌曲流派、年份筛选，标题/艺人/专辑/流派搜索，歌曲排序和专辑、歌曲分页。
- 新增 MusicBrainz 元数据 Provider 与候选匹配接口，为后续中文或私有数据源保留统一的可插拔扩展点。
- 新增本地音乐按需转码接口，可通过已配置的 `ffmpeg` 输出 AAC 或 Opus，并支持 64-320 kbps 音质策略。

### 变更

- 小说入口调整到侧栏媒体库主导航，并紧跟在漫画入口下方；小说使用独立 `BookOpen` 图标，不再与漫画复用图标。
- 小说书架复用 `ContentPageLayout` 和 `ContentZoomSlider`；详情页与阅读器统一使用 HeroUI 原生组件和项目主题 token，支持主题切换。
- 小说阅读内容由后端转换为纯文本章节响应，不直接向浏览器输出 EPUB 内部 HTML。
- 音乐专属表改为应用启动时按当前结构破坏性重建，不再维护 `music_v1`、`music_v2` 等版本表。
- 音乐界面优先改用 HeroUI Tabs、Card、Button、Tooltip、Slider、Chip 等原生组件，并复用 `ContentPageLayout` 与 `ContentZoomSlider`。
- 音乐元数据外部匹配改为仅返回候选与置信度，不会自动覆盖本地标签；用户确认写回和中文数据源接入可在后续 Provider 中扩展。

### 优化

- EPUB 解析和阅读沿用 WebDAV 临时物化与清理流程，避免长期保存远程原始文件副本。
- 小说封面作为派生缓存保存到 `api/data/preview/novels`，并通过受权限保护的 API 提供访问。
- 音乐后台解析、扫描缓存和重试任务共享全局并发上限，避免多个媒体库同时分析时占满 CPU 与 IO。
- 音乐封面支持 JPEG、PNG、WebP 和 GIF；超过 8 MB 的内嵌封面会被忽略，避免异常文件造成资源峰值。
- 音乐专辑会根据仍有效的媒体项重新聚合曲目数，并在曲目删除后清理空专辑及其封面缓存。

## 0.41 - 2026-08-14

### 新增

- 新增首次启动初始化流程，不再创建带固定密码的默认管理员；遗留且仍使用默认密码的 `superadmin` 会在初始化时被安全替换为新的 Owner 账户。
- 新增账户、角色和权限管理，支持 Owner、Admin、Editor、Viewer 四类角色及系统内置权限。
- 新增媒体库级访问授权。Owner 与 Admin 可访问全部媒体库，Editor 与 Viewer 仅能访问创建账户时明确授权的媒体库。
- 新增登录失败限速、会话令牌哈希存储，以及面向 HTTPS 部署的 `COOKIE_SECURE` 配置。
- 新增视频媒体库浏览、视频详情与播放、播放进度、视频分析、封面生成和视频集合能力。
- 新增 WebDAV 远程数据源，可验证连接、扫描远程媒体并代理读取媒体内容。
- 新增结构化运行日志、HTTP Request ID、请求状态和耗时追踪，可通过 `RUST_LOG` 调整日志级别。
- 新增 GitHub Actions CI，自动检查 Rust 格式、测试、Clippy，以及 Web 端 ESLint 和生产构建。
- 新增项目内 FFmpeg/FFprobe 自动发现，支持 `MENGNEX_FFMPEG_DIR`、`api/tools/ffmpeg`、`api/tools/ffmpeg-full_build` 和发行包 `resources/ffmpeg` 目录，同时保留自定义命令路径。
- 新增可自由选色的主题外观设置，支持强调色、基础色、亮暗文字色、字体、组件圆角、表单圆角及主题预设。

### 变更

- 权限检查从宽泛的 HTTP 方法判断调整为按操作分类，媒体整理操作正确使用 `media.write`，系统配置操作使用 `system.manage`。
- 媒体库、媒体文件、照片、视频、漫画、回收站、扫描任务和任务中心的查询均按当前用户可访问媒体库过滤。
- 应用启动时会将因进程重启失去执行器的排队中、运行中或暂停任务标记为失败，避免任务永久占用媒体库。
- API 内部数据库和文件系统错误不再原样返回客户端，详细原因改为写入服务端日志。
- OpenAPI 规范和 Web 端生成客户端已同步账户初始化及媒体库授权字段。
- Web 页面元数据和文档语言更新为 Mengnex 与简体中文，不再保留 Next.js 默认模板信息。
- 为运行时头像和 WebDAV 临时文件补充 Git 忽略规则，并延续预览图和数据库文件的忽略策略。
- 媒体库设置导航重新整理：扫描参数并入媒体库列表弹窗，元数据、作者库和标签库统一收敛到“资源管理”标签页。
- 内容页、设置页和任务中心统一使用共享页面布局，移除重复的旧头部与媒体库布局组件。

### 优化

- 将照片 `preview` 默认生成参数调整为最大 640px、质量 45，继续只保留单份预览资源，降低媒体库磁盘占用与首屏传输量。
- 媒体文件与预览响应增加 ETag/条件请求支持，浏览器重复加载未变化资源时直接返回 304。
- 任务中心使用轻量汇总接口展示全局计数，活动任务轮询不再拉取完整历史任务列表；补充任务状态、视频目录和播放状态索引。
- WebDAV 派生图源文件改为分块写入临时文件，生成照片 Preview 或视频封面后立即清理；原图和音视频仅在查看、播放时按需流式读取，不长期保存原文件副本。
- 视频目录将搜索、集合代表项、观看状态、排序、总数和分页下推 SQLite，仅补查当前页关联数据。
- 照片主图库改用 `before_id` 游标分页，媒体库设置页改用单个封面聚合请求。
- SQLite 连接池限制为 12 个连接，并为每个连接统一配置 WAL、NORMAL synchronous、busy timeout 与外键检查。
- 前端生成代码从 ESLint 业务规则中排除，业务代码恢复零错误、零警告检查。
- 优化照片卡片图片加载失败时的原图回退逻辑，避免来源变化引发额外的同步 Effect 更新。
- 优化视频集合封面渲染，统一使用 Next Image 组件。
- 照片 Preview 渲染采用固定并发上限，避免大量高分辨率图片同时解码造成内存峰值。
- 视频卡片取消悬停自动加载媒体流，避免大规模视频库在浏览列表时产生无意义的 Range 请求。
- 角色权限更新和用户媒体库授权写入使用数据库事务，降低部分写入造成的不一致风险。
- 后端质量门禁升级为 `cargo fmt --check`、完整测试及严格 Clippy `-D warnings`。

### 修复

- 修复扫描与浏览缓存共用一条任务导致阶段和失败状态混淆的问题；扫描成功后会创建独立缓存任务，并按照片缩放或视频抽帧逻辑分派。
- 修复视频浏览缓存全部生成失败时仍显示“已完成 100%”、任务错误原因不可见，以及旧视频封面任务类型未归一的问题。
- 修复任务中心的全部、失败和标签计数只统计当前请求结果的问题；视频技术信息任务现在支持逐项进度、互斥和真实失败状态。
- 修复 Editor 拥有 `media.write` 却无法执行标签、作者头像、照片删除和回收站操作的问题。
- 修复普通用户可以越权读取未授权媒体库及其文件内容、照片、视频、漫画和任务的问题。
- 修复应用重启后后台任务保持运行状态并阻塞后续任务的问题。
- 修复 WebDAV 多状态响应解析器接受未闭合 XML 的问题。
- 修复 WebDAV 路径可能跳出已配置远程根目录，以及响应路径中的 XML 实体未正确还原的问题。
- 修复媒体文件预览变体判断中重复条件的问题。
- 修复任务中心图标数组缺少 React `key`、前端显式 `any` 和多项严格 lint 问题。

## 0.15a - 2026-07-10

### 新增

- 新增 `/photo/folder` 文件夹视图，支持基于图片 `source_path` 构建递归文件夹树，并以混合网格方式同时展示子文件夹与照片。
- 新增照片模块共享头部状态，统一管理导航、搜索与内容缩放，供 `/photo` 与 `/photo/folder` 复用。
- 新增 `/photo/folder` 面包屑导航、文件夹拼图封面、子项计数与当前目录内模糊搜索结果页。
- 新增图片查看器右上角操作栏，支持显示图片信息、下载原图、真实删除图片以及自动隐藏交互。
- 新增照片删除接口 `DELETE /api/photos/{photo_id}`，删除时会同时移除原始文件、预览图、缩略图和数据库记录。

### 变更

- `/photo` 入口从左侧导航切换为顶部导航布局，搜索框与内容缩放滑块并入照片模块头部。
- `/photo` 页面搜索改为专用搜索结果视图，内容缩放改为控制图库图片高度而不是列数。
- `/photo/folder` 页面改为文件夹与照片混合排序展示，并保证子文件夹始终排在照片前面。
- `/photo/folder` 内容缩放逻辑改为控制网格列数，默认第二挡位对应 13 列，左侧更小、右侧更大。
- 文件夹与照片卡片标题从预览容器中拆出，统一使用更轻量的预览图 + 标题结构。
- 图片查看器信息展示由底部浮层改为右上角信息入口与内嵌信息面板，不再依赖鼠标悬停自动显示详情。

### 优化

- 文件夹卡片预览改为使用其下级图片拼图封面，更贴近媒体库卡片的预览方式。
- 文件夹与搜索结果页的网格、头部操作区、面包屑与缩放交互进行了针对桌面浏览的样式整理。
- 图片查看器操作按钮改为更稳定的 HeroUI 按钮布局，修正图标偏移、关闭按钮重复节点和信息面板闪退问题。

### 修复

- 修复 HeroUI 滑块在照片头部中不可拖动的问题，改为稳定的自定义范围滑块实现。
- 修复 `/photo/folder` 面包屑换行、混排顺序异常以及缩放逻辑与图库页不一致的问题。
- 修复图片查看器中信息面板点击后立刻关闭、定位异常以及与全屏查看器遮罩冲突的问题。

## 0.1c - 2026-07-09

### 新增

- 新增 `start.ps1` 一键启动脚本，可在单个终端中同时启动前后端，并在脚本退出时一并停止两个进程。
- 设置页导航升级为一级分组加二级菜单结构，新增 `媒体库列表`、`扫描设置`、`元数据管理`、`系统日志`、`账号安全` 等入口。
- 新增全局扫描设置，可配置缩略图与预览图的最大分辨率和输出质量，并通过 `/api/preferences` 持久化到后端。
- 为照片查看器新增“预览图打底，原图从上到下渐进替换”的大图查看加载效果。

### 变更

- 移除照片显示来源的用户选择项，前端统一改为列表优先使用缩略图，其次预览图；大图查看优先使用原图。
- 设置页中的原“媒体库”调整为二级菜单 `媒体库列表`，原“偏好设置”调整为 `首选项` 分组下的二级菜单。
- 照片查看器由直接显示单一路径改为区分列表图、查看器预览图与原图来源。
- 缩略图与预览图的渲染参数不再写死在代码常量中，改为从应用级设置读取。

### 优化

- 前端主题系统升级，支持亮色、暗色、跟随系统，并为全局背景、导航、头部、侧边栏和任务页等界面补齐主题适配。
- 全局背景改为纯渐变方案，亮色使用抹茶粉渐变，暗色使用紫色渐变，并统一为左上到右下的方向。
- 媒体库管理页全面改造为 HeroUI 风格表单，修复输入框、下拉单选、主题色和长列表滚动表现。
- 媒体库卡片重做布局与主题适配，支持更紧凑的操作区、主题化标签、改进后的照片拼图封面以及可滚动的更多菜单。
- `/tasks` 页面文案已统一调整为中文。

### 修复

- 修复 PowerShell 启动脚本的编码与语法问题，避免 `try/catch` 解析失败和字符串乱码。
- 修复 HeroUI 选择器在错误用法下触发的 `cannot be rendered outside a collection` 等前端报错。
- 修复新建媒体库时输入框无法输入的问题，并统一改用稳定可控的 HeroUI 表单组合。
- 修复媒体库页下拉过长时超出屏幕但无法滚动选择的问题。
- 修复媒体库卡片在亮色主题下标签文字和背景不匹配的问题。
- 修复照片大图查看错误优先使用预览图的问题，恢复为预览图打底后切换原图的设计。

## 0.1b - 2026-07-09

### 新增

- 新增持久化任务表 `app_tasks`，统一承载扫描任务与缩略图/预览图生成任务。
- 新增任务控制接口，支持对后台任务执行暂停、继续、取消。
- 新增扫描任务的实时进度字段，包括 `processed_files` 与 `removed_files`。
- 新增 SQLite 运行时优化配置，包括 `WAL`、`busy_timeout` 与常用索引初始化。
- 新增照片列表与媒体列表的分页能力，后端支持 `limit` / `offset` 查询参数。
- 新增照片页分批加载能力，避免一次性拉取全量照片。

### 变更

- 将媒体库扫描改为真正的后台任务，请求会立即返回任务信息，而不是阻塞到扫描完成。
- 将缩略图生成任务从进程内内存状态迁移为数据库持久化状态，应用重启后任务历史仍可查看。
- 统一任务中心的数据来源，`/api/tasks` 现在直接读取持久化任务表，不再拼接内存任务。
- 媒体库删除前会检查是否仍存在运行中的后台任务，避免在任务执行中删除资源。
- 照片页与媒体库设置页调整为优先按批次加载照片数据，降低大库场景下的首屏压力。

### 优化

- 扫描时改为先按媒体库预加载已有文件索引，再进行路径比对，减少逐文件查库开销。
- 扫描结束后会自动清理磁盘上已删除文件对应的数据库记录与衍生缓存文件，避免索引长期脏化。
- 扫描写入 `media_items`、`media_files`、`photo_assets` 的多步数据库操作已纳入事务，降低半成功状态风险。
- 删除媒体库时，多表删除操作已纳入事务，降低中途失败后留下残缺数据的风险。
- 任务执行循环会感知任务状态，在暂停时等待，在取消时尽快安全退出。

### 修复

- 修复任务中心中扫描任务与缩略图任务状态来源不一致的问题。
- 修复首次创建媒体库后，前端仍按同步扫描完成来提示用户的问题。
- 修复扫描后不会清理失效文件记录的问题。
- 修复缩略图任务在应用重启后状态丢失的问题。

## 0.1a - 2026-07-08

### 新增

- 创建 Rust `api` 服务，包含 Axum、SeaORM、SQLite、`api/data` 数据目录、OpenAPI 输出、Swagger UI，以及面向 Web 客户端的 CORS 配置。
- 增加媒体管理的基础数据结构：媒体库、扫描任务、共享媒体项/文件，以及专用照片资源表。
- 增加照片媒体库的文件系统扫描能力，支持写入数据库并通过 API 端点流式读取本地媒体文件。
- 在 Web 应用中增加首选项和媒体库管理设置页。
- 增加 `@hey-api/openapi-ts` 生成流程与类型安全的前端 API 客户端。
- 更新照片图库页面，通过 API 加载已扫描照片，并使用现有图库与查看器组件展示。
- 新增应用级偏好设置存储与 API，可选择照片默认优先使用缩略图还是原图。
- 新增照片媒体库的预览图缓存生成能力，缓存文件存放在 `api/data/preview`。
- 在 `photo_assets` 中新增缓存元数据字段，用于记录缓存路径、大小和生成时间。
- 新增媒体库手动生成缓存、删除缓存的能力。
- 新增统一任务中心页面，以及 `/api/tasks` 聚合接口，用于集中查看扫描任务和缓存生成任务。
- 新增媒体库设置、信息、删除等弹窗交互。
- 新增 WebP 预览图缓存输出。
- 新增缓存目录占位文件与 Git 忽略规则，避免把实际生成缓存提交到仓库。

### 变更

- API 的 UUID 改为以文本字符串写入 SQLite，避免数据库查看时出现二进制 UUID 内容。
- API 源码结构调整为 `core`、`infra` 和功能 `modules`。
- 将仓库忽略规则集中到根目录 `.gitignore`。
- 照片浏览逻辑改为优先显示预览图缓存，查看器按需加载原图。
- 手动生成缓存改为后台任务执行，并支持进度跟踪，而不是阻塞在设置页面中。
- 媒体库卡片改为聚焦封面与操作，详细统计信息统一移动到“更多 -> 信息”弹窗中。
- 媒体库管理能力扩展为支持重新扫描、启用或停用、重命名、修改根路径，以及配置扫描后自动补齐缓存。
- 手动生成缓存默认只补齐缺失或过期资源，不再强制整库重建。
- 缓存编码从 JPEG 迁移为 WebP，以获得更好的体积和传输表现。
- 全局布局改为顶部头部固定、主内容区独立滚动，避免长内容把侧边栏一并拉伸。

### 修复

- 通过允许本地 IP 图片优化，修复 Next Image 无法加载本地 API 图片的问题。
- 修复前端对 hey-api 生成客户端响应的解包逻辑。
- 将设置页移动到 `(setting)` 路由组下，修复设置路由布局残留问题。
- 修复设置页文案回退为英文的问题，恢复中文界面文案。
- 修复媒体库卡片上操作图标的对齐和悬停态表现。
- 修复媒体库“更多”菜单在点击后打开弹窗时不自动收起的问题。
- 修复侧边栏底部导航位置，使 `任务` 和 `设置` 始终固定在最底部。
- 修复图库、任务列表等长内容页面会连带拉伸侧边栏和头部布局的问题。
